test {
  akka {
    loglevel = "DEBUG"
    stdout-loglevel = "DEBUG"
    actor {
      debug {
//        unhandled = on
//        lifecycle = on
      }
    }
    persistence {
      journal.plugin = "inmemory-journal"
      snapshot-store.plugin = "inmemory-snapshot-store"
    }
  }
}